# AGENTS.md
이 파일은 DURE 저장소에서 Codex가 항상 먼저 읽고 따라야 하는 작업 지침이다.
목표는 빠른 추측보다 일관된 구현, 작은 변경, 검증 가능한 결과다.
모든 md를 매번 읽지 말고 작업 성격에 맞는 문서만 먼저 읽는다.
문서 간 충돌이 있으면 먼저 충돌을 명시하고 사용자에게 확인한다.
페이지 구현 시 UI/UX는 이미 존재하는 디자인을 기준으로 맞춘다.
임의로 새로운 디자인 시스템, 레이아웃 패턴, 색상 체계를 만들지 않는다.
## 1. 작업 전 사고
구현 전에 요청을 한 문장으로 재정의한다.
확실한 가정과 불확실한 가정을 분리해 말한다.
여러 해석이 가능하면 조용히 하나를 고르지 않는다.
불확실성이 결과를 바꿀 정도라면 질문한다.
질문 없이 진행할 수 있는 작은 범위라면 그 범위를 명시하고 진행한다.
더 단순한 해결책이 있으면 먼저 제안한다.
요청보다 큰 구조 변경이 필요해 보이면 이유와 대안을 설명한다.
혼란을 숨기지 않는다.
추측으로 도메인 규칙을 만들지 않는다.
모르는 정책은 문서에서 확인한다.
## 2. 문서 우선순위
용어는 `context.md`를 따른다.
제품 범위와 수락 기준은 `prd.md`를 따른다.
기술 구조와 권한 설계는 `architecture.md`를 따른다.
페이지와 서버 사이의 입력, 출력, 오류, 권한 계약은 `docs/api-spec.md`를 따른다.
DB 테이블, enum, RLS, Storage 정책은 Supabase migration을 따른다.
명칭, 역할, 상태 표현이 헷갈리면 `context.md`를 먼저 읽는다.
기능 범위, 화면 책임, 수락 기준이 필요하면 `prd.md`를 먼저 읽는다.
라우팅, 서버 구조, 권한 설계, 저장소 기준은 `architecture.md`를 먼저 읽는다.
query/action, DTO, 오류 코드, 페이지별 계약은 `docs/api-spec.md`를 먼저 읽는다.
테이블, enum, RLS, Storage 정책 수정은 Supabase migration을 먼저 읽는다.
문서 수정이나 범위 판단은 관련 문서만 읽고 필요할 때 연결 문서를 추가로 확인한다.
API 계약과 migration이 충돌하면 바로 수정하지 말고 충돌을 보고한다.
화면에서 필요한 DTO 필드가 없으면 먼저 API 스펙 수정 필요성을 밝힌다.
문서에 없는 기능은 MVP 범위 밖으로 본다.
이전 디자인 용어가 남아 있어도 구현 용어는 `그룹`, `수업`, `참여자`로 통일한다.
참여자는 로그인 사용자가 아니라 운영 대상 데이터다.
## 3. 단순성 원칙
요청을 만족하는 최소 코드를 작성한다.
요청받지 않은 기능을 추가하지 않는다.
한 번만 쓰는 코드를 위해 추상화를 만들지 않는다.
미래 확장성을 이유로 설정값을 늘리지 않는다.
불가능한 시나리오를 위한 과한 방어 코드를 만들지 않는다.
200줄이 50줄로 가능하면 다시 줄인다.
새 라이브러리는 꼭 필요할 때만 도입한다.
기존 패턴으로 충분하면 기존 패턴을 쓴다.
복잡한 구현을 선택했다면 왜 단순한 방식이 부족한지 설명한다.
코드보다 계약과 데이터 흐름을 먼저 단순하게 만든다.
## 4. 수술적 변경
요청과 직접 연결된 파일만 수정한다.
관련 없는 리팩터링을 하지 않는다.
주변 코드 포맷을 임의로 바꾸지 않는다.
기존 스타일이 마음에 들지 않아도 그대로 맞춘다.
사용자가 만들었을 수 있는 변경을 되돌리지 않는다.
내 변경으로 생긴 unused import, 변수, 함수는 제거한다.
기존 dead code는 발견해도 삭제하지 말고 보고만 한다.
모든 변경 라인은 사용자 요청과 연결되어야 한다.
큰 파일을 건드릴 때는 필요한 부분만 좁게 수정한다.
동작 변경과 정리 작업을 한 번에 섞지 않는다.
## 5. 목표 기반 실행
작업을 검증 가능한 성공 기준으로 바꾼다.
버그 수정은 재현 조건을 먼저 확인한다.
검증 방법이 없으면 가능한 최소 검증 방법을 제안한다.
다단계 작업은 짧은 계획을 세운다.
각 단계에는 확인할 명령이나 결과가 있어야 한다.
테스트가 있으면 먼저 관련 테스트를 찾는다.
테스트를 추가할 수 있으면 실패하는 테스트부터 만든다.
테스트 추가가 과하면 이유를 밝히고 대체 검증을 한다.
검증 전에는 완료됐다고 말하지 않는다.
실패한 검증은 숨기지 않고 원인과 다음 조치를 말한다.
## 6. DURE 구현 규칙
페이지는 Supabase 업무 테이블을 직접 조회하지 않는다.
페이지는 `docs/api-spec.md`의 query/action 계약을 호출한다.
서버 action은 클라이언트가 준 role, userId, memberId를 신뢰하지 않는다.
항상 현재 Auth 사용자와 `workspace_members`를 다시 확인한다.
`workspaceId`는 모든 업무 데이터 요청의 입력에 포함한다.
`can*` 값은 화면 제어용이며 서버에서 다시 권한을 계산한다.
권한 필터는 클라이언트에서 끝내지 않는다.
복잡한 권한은 공통 서비스 함수에 모은다.
최근 활동은 `getRecentActivity` DTO로만 노출한다.
자료 다운로드와 업로드는 signed URL 발급 전에 DB 권한을 확인한다.

## 7. 역할과 권한

대표 운영자는 워크스페이스 전체를 관리한다.
그룹 운영자는 지정된 그룹 범위만 관리한다.
강사는 직접 배정된 수업만 본다.
참여자는 API actor가 될 수 없다.
그룹 운영자는 새 그룹 생성, 비활성화, 삭제를 하지 않는다.
그룹 운영자는 참여자 마스터 전체를 수정하지 않는다.
그룹 운영자의 참여자 제거는 자기 그룹 범위 제거로 처리한다.
강사는 수업 기본 정보와 상태를 수정하지 않는다.
강사는 일반 일정을 보거나 관리하지 않는다.
마지막 대표 운영자는 제거하거나 강등하지 않는다.

## 8. 데이터 보존

기록 도메인은 물리 삭제보다 상태 변경을 우선한다.
참여자 삭제는 기존 출석 기록을 훼손하지 않는다.
그룹 삭제는 기존 수업의 그룹명 스냅샷을 유지한다.
회차에 출석이나 메모가 있으면 삭제하지 않고 숨김 또는 취소로 처리한다.
자료 교체는 자료 레코드를 유지하고 파일만 바꾼다.
자료 제목, 설명, 파일, 공개 범위 변경 시 확인 상태는 `pending`으로 되돌린다.
출석 기록은 회차와 참여자 스냅샷을 보존한다.
운영자를 제거해도 기존 수정자 기록은 보존한다.
소프트 삭제된 데이터는 새 후보 목록에 기본 노출하지 않는다.
기록 화면에는 snapshot 필드를 우선 사용한다.

## 9. 페이지 구현

라우팅은 `architecture.md`의 App Router 구조를 따른다.
홈은 운영 중인 수업 카드와 상세 진입을 제공한다.
일정 관리는 월간 캘린더와 일반 일정만 담당한다.
사용자 초대/권한 설정은 대표 운영자 전용이다.
그룹, 수업, 참여자 관리는 내부 탭 기준을 유지한다.
수업 홈 MVP는 배너와 카드 색상 설정만 제공한다.
수업 자료는 목록, 업로드, 수정, 교체, 다운로드, 확인 상태를 담당한다.
참여자 현황은 출석 집계와 수업 참여자 배정을 담당한다.
강사 콘솔은 수업 홈, 자료, 출석부만 제공한다.
별도 알림센터 페이지는 만들지 않는다.

## 10. 최종 보고

무엇을 바꿨는지 파일 기준으로 짧게 말한다.
검증한 명령과 결과를 말한다.
검증하지 못한 것은 이유를 말한다.
남은 위험이나 결정 필요 사항을 숨기지 않는다.
불필요하게 긴 설명을 피한다.
